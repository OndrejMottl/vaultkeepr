% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_classification_table.R
\name{get_classification_table}
\alias{get_classification_table}
\title{Get the classification table for the data}
\usage{
get_classification_table(con = NULL, return_raw_data = FALSE)
}
\arguments{
\item{con}{A \code{vault_pipe} object created by \code{\link[=open_vault]{open_vault()}}. Must already contain
\code{taxon_id} in the data, i.e. \code{\link[=get_taxa]{get_taxa()}} with
\code{classify_to = "original"} must have been called earlier in the pipe.}

\item{return_raw_data}{A \code{logical}. If \code{FALSE} (default), returns a \code{tibble} with both IDs
and resolved names: \code{taxon_id}, \code{taxon_name}, \code{taxon_species},
\code{species_name}, \code{taxon_genus}, \code{genus_name}, \code{taxon_family}, and
\code{family_name}. This format is human-readable and can also be fed
back as \code{classification_data} in \code{\link[=get_taxa]{get_taxa()}} or \code{\link[=get_traits]{get_traits()}}.
If \code{TRUE}, returns a raw \code{tibble} with only ID columns \code{taxon_id},
\code{taxon_species}, \code{taxon_genus}, and \code{taxon_family} matching the
\code{TaxonClassification} schema.}
}
\value{
When \code{return_raw_data = FALSE} (default): a \code{tibble} with eight
columns — \code{taxon_id}, \code{taxon_name}, \code{taxon_species}, \code{species_name},
\code{taxon_genus}, \code{genus_name}, \code{taxon_family}, \code{family_name} —
restricted to taxa present in the data. Both IDs and resolved names
are included, so the tibble can be inspected directly and also passed
to the \code{classification_data} argument of \code{\link[=get_taxa]{get_taxa()}} or \code{\link[=get_traits]{get_traits()}}.
When \code{return_raw_data = TRUE}: a \code{tibble} with columns \code{taxon_id},
\code{taxon_species}, \code{taxon_genus}, and \code{taxon_family} restricted to
the species-level taxon IDs present in the data.
}
\description{
Extract the \code{TaxonClassification} table for all taxa present in the
current dataset. The result is a collected \code{tibble} (not a \code{vault_pipe})
and is intended as a "pipeline split": the user can inspect, edit, and
pass the result back into \code{\link[=get_taxa]{get_taxa()}} or \code{\link[=get_traits]{get_traits()}} via their
\code{classification_data} argument.
}
\details{
This function must be called after \code{\link[=get_taxa]{get_taxa()}} with
\code{classify_to = "original"} so that the data contains species-level
\code{taxon_id} values that match the \code{TaxonClassification} table.
Calling it after a higher-level classification (e.g. \code{"genus"}) will
yield an empty result because the genus IDs are not primary keys in
\code{TaxonClassification}.

Typical workflow:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{con_taxa <-
  open_vault(path) |>
  get_datasets() |>
  get_samples() |>
  get_taxa(classify_to = "original")

data_class <-
  get_classification_table(con_taxa, return_raw_data = TRUE)

# inspect / edit data_class, then feed back:
con_taxa |>
  get_taxa(
    classify_to = "genus",
    classification_data = data_class
  )
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=open_vault]{open_vault()}}, \code{\link[=get_taxa]{get_taxa()}}, \code{\link[=get_traits]{get_traits()}}
}
