% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_taxon_names.R
\name{get_taxon_names}
\alias{get_taxon_names}
\title{Get taxon names from a vault plan}
\usage{
get_taxon_names(con = NULL)
}
\arguments{
\item{con}{A \code{vault_pipe} object. Must have been passed through \code{get_taxa()} so
that a \code{taxon_id} column is present in \code{con$data}.}
}
\value{
A \code{tibble} with a single column \code{taxon_name} containing the distinct,
alphabetically sorted taxon names available in the current query plan.
}
\description{
Retrieve a tibble of distinct taxon names from the current \code{vault_pipe}
query plan without executing a full \code{extract_data()} call. Requires
\code{get_taxa()} to have been called earlier in the pipe so that \code{taxon_id}
is present in the data.
}
\seealso{
\code{\link[=get_taxa]{get_taxa()}}, \code{\link[=select_taxa_by_name]{select_taxa_by_name()}}
}
